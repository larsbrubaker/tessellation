{
  "name": "tessellation",
  "version": "0.9.0",
  "private": true,
  "scripts": {
    "build:wasm": "cd demo/wasm && wasm-pack build -d ../public/pkg --target web",
    "dev": "bun run build:wasm && cd demo && bun run dev"
  }
}
